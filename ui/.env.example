# Base URL of the LexBot API. The client defaults to http://localhost:8000
# when this is unset. Vite only reads env files from the Vite project root,
# so this file (ui/.env.example) is the operative template for the UI.
VITE_API_URL=http://localhost:8000